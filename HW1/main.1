//計算字元出現次數
//Description
//計算一個字串內，連續出現的字元與其出現次數
//Input
//請設計輸入 (scanf) 一個連續的字串，不包含空白字元
//*總字元數請控制在100字以內*
//Output
//輸出 (printf) 字元與其出現次數
//Sample Input 1 
//AABBBCCCCddd\n
//Sample Output 1
//A2B3C4d3\n
//Sample Input 2 
//ABCD\n
//Sample Output 2
//A1B1C1D1\n
#include <stdio.h>

int main() {
    char input[101];
    
    scanf("%s", input);

    // 遍歷字串並計算出現次數
    int count = 1;
    for (int i = 0; input[i] != '\0'; i++) {
        if (input[i] == input[i + 1]) {
            count++;
        } else {
            printf("%c%d", input[i], count);
            count = 1;
        }
    }

    return 0;
}
