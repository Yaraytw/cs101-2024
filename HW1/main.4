//字串反轉
//Description
//輸入(scanf)一個字串，將其順序反轉輸出 (printf)
//Input
//包含大小寫英文字母與數字的組合
//Output
//包含大小寫英文字母與數字的組合
//Sample Input 1 
//Hello\n
//Sample Output 1
//olleH\n
//Sample Input 2 
//ABC123\n
//Sample Output 2
//321CBA\n

#include <stdio.h>
#include <string.h>

int main() {
    char input[100];  // 假定輸入的字串長度不超過100個字符
    
    scanf("%s", input);

    int length = strlen(input);

    // 反轉字串
    for (int i = 0; i < length / 2; i++) {
        char temp = input[i];
        input[i] = input[length - 1 - i];
        input[length - 1 - i] = temp;
    }

    // 輸出反轉後的字串
    printf("%s\n", input);

    return 0;
}
